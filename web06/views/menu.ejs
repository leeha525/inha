<div class="mt-5">
    <a href="/" class="me-3">Home</a>
    <a href="/kakao/book" class="me-3">도서검색</a>
    <a href="/kakao/local" class="me-3">지역검색</a>
    <a href="/kakao/blog" class="me-3">블로그검색</a>

    <a id="login" href="/users/login" class="me-3" style="float:right">로그인하기</a>
    <!--"#" : 현재 페이지에서 이동하지 않음-->
    <a id="logout" href="#" class="me-3" style="float:right">로그아웃</a>
    <a id="mypage" href="/users/mypage" class="me-3" style="float:right">마이페이지</a>

    <hr>
  </div>

  <script>
    // 로그인 후 로그인 링크 대신 아이디를 나타나게 하기 위해서 이름 가져오기
    // sessionStorage에 저장해두었던 email 호출
    const email = sessionStorage.getItem("email");

    if(email) { // 이메일이 있다면
      // 로그인 상태
      $("#login").hide();
      $("#mypage, #logout").show();
      $("#mypage").html(email);
    }else {
      // 로그아웃 상태
      $("#login").show();
      $("#mypage, #logout").hide();
    }

    $("#logout").on("click", function(e) {
      e.preventDefault();
      // confirm : 물어보기
      if(confirm("정말로 로그아웃 하시겠습니까?")) {
        sessionStorage.clear();
        // session 정보 삭제 후 메인페이지로 이동
        location.href="/";
      }
    });
  </script>